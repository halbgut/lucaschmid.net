<navigation>
  <ul>
    <li each={ items }><a href="#{ id }">{ name }</a></li>
  </ul>
  <script>
    //console.log(querySelectorAll('h1'))
    //this.items = [].map.call(querySelectorAll('h1'), (el) => {
    //  return { id: el.id, name: el.innerHTML }
    //})
    this.items = [{ id: '#el.id', name: 'el.innerHTML' }]
  </script>
</navigation>

